All data can be changeable from `data` directory

1. Install Dependencies

```sh
yarn
```

2. Bundling Dependencies

```sh
yarn build
```

Starting in development mode

```sh
yarn dev
```

Starting in production mode

```sh
yarn build
```
